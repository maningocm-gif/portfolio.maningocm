# Chariza Mae Portfolio Website

This is a static GitHub Pages website that displays the Canva portfolio as an image, rather than opening a PDF viewer.

## Publish
1. Upload `index.html` and `portfolio.png` to the root of a GitHub repository.
2. Open **Settings → Pages**.
3. Select **Deploy from a branch**, branch `main`, folder `/(root)`, then Save.
